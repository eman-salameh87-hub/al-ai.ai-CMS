node_modules/
.next/
out/
build/
dist/

.env
# Only the literal name .env was ignored, so .env.local and any
# .env.<something> holding credentials showed up in git status.
.env.*
!.env.example
.env.local
.env.*.local

# Local uploads — user content, not source
public/uploads/

# Rendered mail from the log driver — holds real customer names and addresses
.mail-outbox/

coverage/
playwright-report/
test-results/
# Saved admin session for the browser suite — a real login cookie.
e2e/.auth/

.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

npm-debug.log*
*.tsbuildinfo
next-env.d.ts

# Working files from this session
cookies.txt
c[0-9].txt
ce.txt

# curl cookie jars from local API testing
*.cookies
shop.txt

# Scratch files from local verification runs
.tmp-verify/

# Local-only working notes: the AI/chat dumps the codebase was drafted from,
# superseded design notes, and curl cookie jars from manual API testing.
# Kept on disk for reference, deliberately out of the repo and out of any
# client copy. Write throwaway cookie jars in here, not at the repo root —
# ca/cb/cc.txt were committed with live admin tokens in them.
prompt-archive/

# Legacy migration inputs. The SQL Server backup is 112 MB of production data —
# customer enquiries and job applications included — and must never be in git.
/migration/legacy-db/*.bak
/migration/legacy-db/*.rar
/migration/out/
